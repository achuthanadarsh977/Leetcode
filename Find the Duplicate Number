/**
 * @param {number[]} nums
 * @return {number}
 */
var findDuplicate = function(nums) {
    
const count = nums.reduce((acc,num)=> {acc[num]  = (acc[num] || 0) + 1;return acc},{})

for (let con in count){
    if(count[con] > 1){
        return Number(con)
        break;
    }

}


};
