function reverse(x: number): number {
    const y = Math.abs(x).toString().split("").reverse().join("")

    const z = parseInt(y,10)
    if(z > Math.pow(2,31) - 1) return 0;

    return x < 0 ? - z : z

};
