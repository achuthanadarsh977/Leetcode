class Solution(object):
    def removeDuplicates(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = sorted(set(nums))
        nums[:len(n)] = n
        return len(n)
